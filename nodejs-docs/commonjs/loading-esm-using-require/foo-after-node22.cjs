// this example works in Node.js versions >= v22

// we can now just require a mjs module
const distance = require('./distance.mjs')

console.log(distance)

const point = require('./point.mjs')

console.log(point)
